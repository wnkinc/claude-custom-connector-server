# Deliberately empty: squid.compose.conf includes this file as the extension
# hook. An overlay repo mounts its own conf over the same container target
# (/etc/squid/conf.d/local.conf) to add per-tool listeners, acls, and allows
# without forking the wall's policy file.
