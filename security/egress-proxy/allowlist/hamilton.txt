# hamilton egress allowlist (squid dstdomain). One host per line; leading dot = subdomains.
# Catalog tool: math/deps are installed at build time; bge-m3 (future) is loopback.
# Only the Google OAuth hosts are needed to serve Claude:
accounts.google.com
oauth2.googleapis.com
www.googleapis.com
openidconnect.googleapis.com
