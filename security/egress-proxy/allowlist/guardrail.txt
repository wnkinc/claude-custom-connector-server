# guardrail sidecar egress allowlist (squid dstdomain). One host per line; a
# leading dot matches subdomains.
#
# llamafirewall provider only: pull the gated PromptGuard model from Hugging
# Face on first start (HF_TOKEN set). After the model is cached in the
# guardrail-hf-cache volume, scans run fully offline.
#
# The bedrock provider's endpoint is NOT listed here — it's region-specific, so
# squid.compose.conf carries it as a dstdom_regex ACL matching every region.
.huggingface.co
.xethub.hf.co
