# lean egress allowlist (squid dstdomain). One host per line; leading dot = subdomains.
# Backtests run entirely against local data (/Lean/Data in the image) -- the engine
# itself needs NO egress. Only the Google OAuth hosts for auth-on serving are allowed;
# discover any misses via squid TCP_DENIED in the egress log
# (docker compose exec egress tail /var/log/squid/access.log).
accounts.google.com
oauth2.googleapis.com
www.googleapis.com
openidconnect.googleapis.com
