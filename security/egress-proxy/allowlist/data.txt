# data egress allowlist (squid dstdomain). One host per line; leading dot = subdomains.
# Keep this to the MINIMUM. Discover misses from TCP_DENIED in the egress log
# (docker compose exec egress tail /var/log/squid/access.log).

# Tiingo — OpenBB tiingo provider REST API (the crypto ingest source).
api.tiingo.com

# Google OAuth — the public instance verifies tokens + fetches JWKS server-side.
accounts.google.com
oauth2.googleapis.com
www.googleapis.com
openidconnect.googleapis.com
