# Provenance: pin ALL deps so a fresh `pip install -r` reproduces a
# known set, not latest -- a floating dep silently un-audits itself on reinstall.
# Versions below = the currently-installed set; OSV/deps.dev vetting is still TODO
# (the deferred dep-vetting pipeline). Re-vet on any version bump.
#
# MCP framework + shared OAuth/.env plumbing:
fastmcp==3.4.2
mcp==1.28.0
python-dotenv==1.2.2
# Data engine. pandas is the in-memory frame; pyarrow is its parquet reader/writer
# (bars persist via DataFrame.to_parquet / read_parquet -- no separate store engine).
pandas==3.0.3
pyarrow==24.0.0
# OpenBB market-data: core framework + the crypto command extension + the tiingo
# provider extension. Crypto-only on purpose: this tool feeds the lean tool, whose
# exporter is crypto-only today (equity/fx extensions + databento were removed in the
# lean refit; recover from git history if the equity fork lands). After adding or
# removing an extension, prebuild the accessor: `python -c "import openbb"`. tiingo
# needs TIINGO_API_KEY.
openbb-core==1.6.13
openbb-crypto==1.6.2
openbb-tiingo==1.6.1
