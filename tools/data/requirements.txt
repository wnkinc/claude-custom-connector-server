# MCP server framework + shared OAuth/.env plumbing.
fastmcp
python-dotenv
# Ingest pipeline deps (same set the secure-agentic data runner uses). These are
# the heavyweight bits the two-process split existed to isolate; here they live
# only in this tool's venv.
pandas>=2.2
pyarrow>=16
yfinance>=0.2.40
