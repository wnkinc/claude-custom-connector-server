# THREAT-MODEL L5 (provenance): pin ALL deps so a fresh `pip install -r` reproduces a
# known set, not latest -- a floating dep silently un-audits itself on reinstall.
# Versions below = the currently-installed set; OSV/deps.dev vetting is still TODO
# (the deferred dep-vetting pipeline). Re-vet on any version bump.
#
# MCP framework + shared OAuth/.env plumbing:
fastmcp==3.4.2
mcp==1.28.0
python-dotenv==1.2.2
# Data engine. pandas is the in-memory frame; pyarrow is its parquet reader/writer
# (bars persist via DataFrame.to_parquet / read_parquet -- no separate store engine).
pandas==3.0.3
pyarrow==24.0.0
# OpenBB market-data: core framework + command extensions (one per data type:
# equity, crypto) + the yfinance provider extension. Commands and providers are SEPARATE
# extensions on purpose; adding a source = another provider ext, adding a data type =
# another command ext (mirrored by a fn in feeds.py + a tool in server.py). yfinance is
# TRANSITIVE (pulled by openbb-yfinance), so it is not listed here. After install/adding
# an extension, prebuild the accessor: `python -c "import openbb"`.
openbb-core==1.6.13
openbb-equity==1.6.2
openbb-crypto==1.6.2
openbb-yfinance==1.6.3
