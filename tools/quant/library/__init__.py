"""The library: engine-agnostic layer-1–3 pieces (indicators / features / signals).

These are plain Python functions. Hamilton reads them and wires them into a DAG by
matching parameter names to function names. Files here are for human navigation
only — Hamilton flattens everything into one namespace and queries by tag.
"""
