fastmcp
sf-hamilton
# numeric + indicator stack (pandas-ta supplies the indicator math)
numpy==2.2.6
pandas==2.3.3
pandas-ta==0.4.71b0
# backtest engine + lake reader; vectorbt pins the numba/llvmlite JIT stack so the
# numeric build is reproducible rather than re-resolved on each install.
vectorbt==1.0.0
numba==0.61.2
llvmlite==0.44.0
scipy==1.18.0
pyarrow==24.0.0
