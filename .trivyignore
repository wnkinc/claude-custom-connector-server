# CVE-2026-39822 (Go stdlib os.Root symlink traversal, fixed in go1.26.5):
# flagged in cloudflare/cloudflared, whose latest release (2026.7.1) is still
# built with go1.26.4 -- no patched upstream build exists yet. Not reachable in
# our deployment: cloudflared runs as a tunnel client with a single read-only
# creds mount. Expiry forces a re-check; drop this entry once a cloudflared
# release built with go >= 1.26.5 is pinned.
CVE-2026-39822 exp:2026-08-15
